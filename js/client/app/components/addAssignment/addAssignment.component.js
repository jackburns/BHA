import template from './addAssignment.html';
import controller from './addAssignment.controller';

let addAssignmentComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default addAssignmentComponent;
